const express = require("express");
const router = express.Router();

const email = require("../controllers/email.controller");

//router.post("/new_email", email.new_email);

module.exports = router;